// setting the "loading" attribute to "lazy" when using iframe.

// This way, the browser delays the loading of the offscreen iframes until the user scrolls near them

<iframe
  src="https://example.com"
  loading="lazy"
  width="100"
  length="100"
  frameborder="0"
></iframe>;
